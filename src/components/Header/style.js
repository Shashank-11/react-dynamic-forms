import styled from 'styled-components'
import {gradients} from '../../constants/css'

export const HeaderContainer = styled.header`
  background-image: ${gradients.headerGradient};
  z-index: 31;
  height: 50px;
  left: 0;
  position: fixed;
  right: 0;
  top: 0;
`
